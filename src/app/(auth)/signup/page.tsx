import { SignupForm } from "@/components/auth/SignupForm";

export const metadata = {
  title: "Create Account — Aura",
  description: "Join Aura. Express. Connect. Evolve.",
};

export default function SignupPage() {
  return <SignupForm />;
}

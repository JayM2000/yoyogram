import { LoginForm } from "@/components/auth/LoginForm";

export const metadata = {
  title: "Login — Aura",
  description: "Welcome back to Aura. Sign in to your account.",
};

export default function LoginPage() {
  return <LoginForm />;
}
